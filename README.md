# glibc ABI Report

This directory contains HTML describing glibc ABI history.

The report is generated from the glibc-abi-tool project in
https://github.com/indygreg/toolchain-tools. e.g.
`cargo run -p glibc-abi-tool -- glibc-symbol-report /path/to/glibc/git/clone /path/to/output/directory`.
